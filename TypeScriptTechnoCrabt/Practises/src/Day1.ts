let course: string = "Next Level DEvelopement";

console.log(course);

const name1: string = "Rahul";
console.log(name1);

const friends: string[] = ["Rahul Rudrs"];

const coordinstes: [string, number] = ["rahul", 89];
console.group(coordinstes);

const userName: {
  firstName: string;
  middleName?: string;
  lastName: string;
} = {
  firstName: "Rahul",
  lastName: "Rudra"
};

const animal: {
  name: "rA";
  isDog?: boolean;
} = { name: "rA" };
